const { funSum, objectSum } = require('./logic');
describe('Test - app', () => {

  beforeEach(() => { });
  afterEach(() => { });

  it('check sum of 1+1=2', () => {
    expect(1 + 1).toEqual(2);
  });
});